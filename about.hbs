# Third-party licenses

The `lamco-wayland` crates bundle the following third-party Rust crates (with all
features enabled, including the PipeWire FFI tree and the ashpd/zbus D-Bus
stack). Each is distributed under the terms of the license shown below.
(Generated by `cargo-about` from the lockfile; refresh with
`cargo about generate about.hbs --all-features`.)

## Overview

{{#each overview}}
- {{name}}: {{count}} crate(s)
{{/each}}

{{#each licenses}}
---

## {{name}}

Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}}
{{/each}}

```
{{{text}}}
```

{{/each}}
